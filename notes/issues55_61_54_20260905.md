# Implementation record — #55, #61, #54 (2026-09-05)

Base: `28cbaa5`. Work is uncommitted. No database rebuilt, merged or refrozen; no input annotation, `evidence/`, `*.sbatch` or protocol file edited. All frozen-database access used DuckDB **`read_only=True`**; the report creates only temporary relations. Original reference GFFs were read and SHA256-checked against `build_manifest`. No write connection to the frozen database was opened.

## #55

Implemented canonical ordering of emitted feature blocks, shared with the validator through `canonicalize_v3`; improved equal-span diagnostics; tests cover both input orders, RC, and annotation spans whose order differs from emitted feature spans. The report audits every recorded ordering rejection and retains its raw message.

**The requested “97 are exactly tie cases” assertion is false.** Read-only measurement finds 97 ordering rejections, **13 equal-span tie breaks and 84 decreasing spans**, with zero unparsed/unexpected cases. Species totals reproduce 8 Athaliana / 10 Ppatens / 1 Ptrichocarpa / 78 Vvinifera. The broader mismatch is annotation-coordinate sorting versus emitted-feature-coordinate sorting. A tie-break-only fix would miss the decreasing spans. The audit proves the recorded failure classes, not reconstructed discarded labels or recovery through later filters.

Deferred to author: whether to replace the frozen corpus to recover ordering failures or accept the omissions for this corpus. Estimated full rebuild budget ~1.5 hours plus merge/validation/refreeze; coordinate with #56. Details and amendment requirement are in `author_decisions_20260905.md`. No dependence on #58 for the forward ordering implementation.

Passing test command:

```sh
python -m pytest -q tests/test_gsf_v3_windows.py tests/test_build_b5.py
```

```text
48 passed in 7.18s
```

## #61

Implemented total unique-gene coverage, strict-holdout counts, gene-split by tile-split matrix, test-gene length distributions and optional hash-verified reference transcript distributions. Existing integrity checks now require matching RC orientation; adversarial tests verify forbidden train/valid labels, strict holdouts and orphan RC membership. Supplementary CSV export uses the same report. The Methods insert separates corpus coverage, all test-tile targets, batch-loss averaging, full-GFF and evidence-callability denominators.

Read-only output: nominal test **15,005 / 91,787 (16.348%)**; valid **7,316 / 29,153 (25.095%)**; train **196,592 / 213,702 (91.994%)**. Strict holdouts **431 / 3,430**. Test-tile labels cover **54,188** unique genes (34,606 train + 4,577 valid + 15,005 test). All three membership assertions pass; reference transcript audit has zero missing test keys. These tests concern labels, not #56's sequence exposure.

Deferred: selecting/redefining the paper's scoring universe. #58 remains necessary for an exact empty-target token convention; #56 remains necessary for a repaired corpus. Neither blocks these measured existing-corpus counts. Distribution differences are reported without inferring representativeness.

Passing test command (including reference-GFF hash rejection):

```sh
python -m pytest -q tests/test_report_b5_database.py tests/test_build_b5.py::test_population_reference_counts_and_hash_guard
```

```text
6 passed in 1.01s
```

## #54

Implemented `scripts/report_swissprot_mapping.py`, per-species supplementary counts and provenance hashes; copied the existing 571-row / 567-gene Arabidopsis detail audit byte-for-byte into the supplement. The Methods insert reports missing rice RAP–MSU mapping, 3,784 / 5,161 exact-sequence mappings and zero hard flags. It also explicitly reports the one soybean hard-flagged gene. All nine flag hashes agree with the frozen manifest; counts distinguish caution events, deduplicated flags, detail-audit rows and unique genes.

No new author decision needed: existing decisions not to pursue the rice map and to retain all Arabidopsis caution masks were followed. The copied detail table covers Arabidopsis, while the mapping table includes all nine species; soybean is not silently omitted.

Test command/output (partition/count/export integrity, frozen flag hash guard and existing sensitivity tests):

```sh
python -m pytest -q tests/test_report_swissprot_mapping.py tests/test_swissprot_sensitivity.py
```

```text
11 passed in 0.51s
```

Export command, after read-only SCP of the existing `swissprot_v3` summary and nine flag TSVs to `/tmp/transgenic-swissprot-inputs`:

```sh
python scripts/report_swissprot_mapping.py --summary /tmp/transgenic-swissprot-inputs/swissprot_summary.json --flags-dir /tmp/transgenic-swissprot-inputs --freeze data/freeze/b5_full_v1.freeze.json --masked-audit ../evidence/swissprot_hard_masked_genes_20260902.tsv --out-dir revision/supplemental_tables
```

```json
{"detailed_audit_genes": 567, "detailed_audit_rows": 571, "frozen_flag_hashes_verified": 9, "hard_flag_genes": 568, "species": 9}
```

## Frozen audit reproduction

The current report and GSF module were staged under `/tmp/transgenic-issues55-61-54` on gpu (no installed code or shared build workspace changed). Executed:

```sh
ssh gpu '~/micromamba/envs/transgenic/bin/python /tmp/transgenic-issues55-61-54/scripts/report_b5_database.py --db /home/pgl/scratch1/wyim/transgenic_data/b5/merged/b5_full_v1.db --reference-distributions --out /tmp/transgenic-issues55-61-54/report.json > /tmp/transgenic-issues55-61-54/report.stdout'
```

Exit 0; JSON copied to `notes/b5_population_audit_20260905.json`. All 11 checks true; 97 ordering rejections = 13 ties + 84 decreasing spans; reference transcript missing keys = 0. All counts reported above are from that output. To export the supplementary CSVs directly during a future audit, add `--tables-dir <output-directory>`; the checked-in tables were rendered from that same JSON using `export_population_tables`.


## Final verification

```sh
python -m pytest -q
```

```text
499 passed, 6 skipped in 32.98s
```

Final per-issue checks above also passed after the supplementary CSV export wiring. `git diff --check` passed. `cmp` confirmed the supplementary detail TSV is byte-identical to the read-only source in outer `evidence/`. No protocol or `*.sbatch` diff; no commits created.
